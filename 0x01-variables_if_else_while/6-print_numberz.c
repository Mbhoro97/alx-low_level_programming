#include <stdio.h>
/**
 * main - prints digit numbers of base 10 starting from 0
 * Return: Always 0 (Succesful)
 */
int main(void)
{
	int i;

	for (int = 48; i < 58; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
